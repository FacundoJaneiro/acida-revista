/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/acida-revista',
  assetPrefix: '/acida-revista/',
  images: { unoptimized: true },
};

export default nextConfig;
